services:
  # Single web service for both backend API + frontend static files
  - type: web
    name: sas-app
    env: node
    region: singapore  # Closest to India
    plan: free
    buildCommand: bash render-build.sh
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000  # Render's default port
      - key: MONGODB_URI
        sync: false  # Set to: mongodb+srv://spreadasmile22_db_user:DYRtTZl0bxwXQc74@sas.qs5tfqz.mongodb.net/sas_database?retryWrites=true&w=majority&appName=SAS
      - key: JWT_SECRET
        value: fU+Fy1A/maec1Y/PFIN3kYK2dSVFqCHDO5ykjSdQrjI=
      - key: JWT_EXPIRE
        value: 30d
      - key: EMAIL_SERVICE
        value: gmail
      - key: EMAIL_HOST
        value: smtp.gmail.com
      - key: EMAIL_PORT
        value: 587
      - key: EMAIL_USER
        sync: false  # Set to: spreadasmile22@gmail.com
      - key: EMAIL_PASSWORD
        sync: false  # Generate Gmail App Password at https://myaccount.google.com/apppasswords
      - key: FROM_NAME
        value: Spread A Smile
      - key: FROM_EMAIL
        sync: false  # Set to: spreadasmile22@gmail.com
      - key: FRONTEND_URL
        sync: false  # Set after first deploy (e.g., https://sas-app.onrender.com)
    # Persistent disk for file uploads (1GB free tier)
    disk:
      name: uploads-disk
      mountPath: /opt/render/project/src/backend/uploads
      sizeGB: 1
    # Auto-deploy when you push to GitHub
    autoDeploy: true
